apiVersion: v1
kind: Service
metadata:
  name: {{ include "cardinal-chart.fullname" . }}
  labels:
    {{- include "cardinal-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cardinal-chart.selectorLabels" . | nindent 4 }}
