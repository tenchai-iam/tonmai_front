apiVersion: v1
kind: Service
metadata:
  name: {{ include "vegx-chart.fullname" . }}
  labels:
    {{- include "vegx-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vegx-chart.selectorLabels" . | nindent 4 }}
